export const metadata = {
    title: "Free CSS Minifier | ToolsHub",
    description: "Minify your CSS code to reduce file size. Free online CSS minifier.",
};

export default function CSSMinifierLayout({ children }) {
    return children;
}
