export const metadata = {
    title: "Free QR Code Reader | ToolsHub",
    description: "Scan and decode QR codes from images. Free online QR code scanner.",
};

export default function QRReaderLayout({ children }) {
    return children;
}
