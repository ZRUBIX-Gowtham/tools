export const metadata = {
    title: "Free Image Resizer | ToolsHub",
    description: "Resize your images to any dimension. Fast and easy, all in browser.",
};

export default function ResizerLayout({ children }) {
    return children;
}
