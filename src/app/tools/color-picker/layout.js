export const metadata = {
    title: "Free Color Picker | ToolsHub",
    description: "Pick colors from images or choose from the palette. Free online color picker tool.",
};

export default function ColorPickerLayout({ children }) {
    return children;
}
