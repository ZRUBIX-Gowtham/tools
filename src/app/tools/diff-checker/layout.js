export const metadata = {
    title: 'Diff Checker - Compare Text & Code Online',
    description: 'Free online diff checker to compare text or code. Highlight differences with a visual side-by-side or inline comparison.',
}

export default function Layout({ children }) {
    return children;
}
