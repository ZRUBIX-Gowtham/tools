export const metadata = {
    title: 'Diff Checker - Compare Text Differences',
    description: 'Online Diff Checker tool to compare two text files or strings and find the differences instantly.',
}

export default function Layout({ children }) {
    return <>{children}</>;
}
