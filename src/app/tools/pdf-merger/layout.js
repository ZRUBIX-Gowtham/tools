export const metadata = {
    title: "Free PDF Merger | ToolsHub",
    description: "Combine multiple PDF files into one document. Free online PDF merge tool.",
};

export default function PDFMergerLayout({ children }) {
    return children;
}
