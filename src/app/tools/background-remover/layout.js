export const metadata = {
    title: "Free Background Remover | ToolsHub",
    description: "Remove background from your images automatically. Free online background removal tool.",
};

export default function BackgroundRemoverLayout({ children }) {
    return children;
}
