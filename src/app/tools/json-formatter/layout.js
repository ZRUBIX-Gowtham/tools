export const metadata = {
    title: "Free JSON Formatter | ToolsHub",
    description: "Format and validate your JSON data. Free online JSON formatter and validator.",
};

export default function JSONFormatterLayout({ children }) {
    return children;
}
