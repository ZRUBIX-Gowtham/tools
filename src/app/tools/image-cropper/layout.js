export const metadata = {
    title: "Free Image Cropper | ToolsHub",
    description: "Crop your images to the perfect size. Free online image cropping tool.",
};

export default function ImageCropperLayout({ children }) {
    return children;
}
