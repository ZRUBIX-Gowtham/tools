export const metadata = {
    title: "Free Gradient Generator | ToolsHub",
    description: "Create stunning CSS gradients for your projects. Free online gradient generator.",
};

export default function GradientGeneratorLayout({ children }) {
    return children;
}
