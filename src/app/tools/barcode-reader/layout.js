export const metadata = {
    title: "Free Barcode Reader | ToolsHub",
    description: "Scan and decode barcodes from images. Free online barcode scanner supports EAN, UPC, Code 128, and more.",
};

export default function BarcodeReaderLayout({ children }) {
    return children;
}
