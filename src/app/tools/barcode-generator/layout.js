export const metadata = {
    title: "Free Barcode Generator | ToolsHub",
    description: "Generate various types of barcodes. Free online barcode generator.",
};

export default function BarcodeGeneratorLayout({ children }) {
    return children;
}
