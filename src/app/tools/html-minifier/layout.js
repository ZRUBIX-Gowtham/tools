export const metadata = {
    title: "Free HTML Minifier | ToolsHub",
    description: "Minify your HTML code to reduce file size. Free online HTML minifier.",
};

export default function HTMLMinifierLayout({ children }) {
    return children;
}
