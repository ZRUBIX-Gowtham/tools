export const metadata = {
    title: "Free Base64 Encoder/Decoder | ToolsHub",
    description: "Encode or decode Base64 strings instantly. Free online Base64 tool.",
};

export default function Base64Layout({ children }) {
    return children;
}
